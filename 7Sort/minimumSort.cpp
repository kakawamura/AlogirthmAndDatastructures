#include <iostream>
#include <stdio.h>

using namespace std;

const int MAX = 1001;

int main() {
    int n, w[MAX];
    scanf("%d", &n);
    for(int i = 0; i < n; i++) {
        scanf("%d", &w[i]);
    }


    return 0;
}

